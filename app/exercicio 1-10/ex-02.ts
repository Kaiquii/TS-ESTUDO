/* A função recebe um parâmetro do tipo number e retorna o resultado da multiplicação por 2. */

function multiplicacao(valor: number) {
  return valor * 2;
}

const valor = multiplicacao(4);
console.log(valor);
