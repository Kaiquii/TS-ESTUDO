/* Crie uma função que recebe um número e retorna seu antecessor. Por exemplo, se o número for 5, a função deve retornar 4 */

function antecessor(valor1: number) {
  return valor1 - 1;
}

const valor = antecessor(5);
console.log(valor);
